---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Axide — Manifesto</title>
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&family=Space+Grotesk:wght@600;700&display=swap" rel="stylesheet">
	</head>
	<body>
		<!-- Hero Section with Logo Space -->
		<section class="hero">
			<div class="logo-space">
				<div class="logo-container">
					<img src="/favicon.svg" alt="Axide Logo" class="logo" />
					<span class="spark"></span>
					<span class="spark spark--2"></span>
				</div>
			</div>
			<div class="hero-content">
				<h1>The Axide<br><span class="accent">Manifesto</span></h1>
			</div>
			<div class="diagonal-band"></div>
		</section>

		<!-- Long-form Content Section -->
		<main class="content-wrapper">
			<article class="prose">
				<p class="lead">
					This is where your journey begins. A lead paragraph that sets the tone for the entire manifesto. Use the <span class="accent">accent</span> class for emphasis on key words.
				</p>

				<h2>The First Movement</h2>
				<p>
					Start writing your long-form content here. The typography is optimized for readability with a generous line height and Inter font. You can break your text into multiple paragraphs to maintain the "Silence Mode" philosophy—one section, one idea.
				</p>

				<p>
					Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.
				</p>

				<blockquote>
					"A single powerful statement to break the rhythm of the text."
				</blockquote>

				<h2>The Diagonal Path</h2>
				<p>
					Use subheadings to organize your thoughts. The Space Grotesk font provides a technical yet dynamic feel to the headers.
				</p>

				<ul>
					<li>First key point of your manifesto</li>
					<li>Second point with <span class="accent">momentum</span></li>
					<li>Third point that embraces the void</li>
				</ul>

				<p>
					Continue your narrative here. The container width is restricted to ensure optimal line length for reading, surrounded by the "Silence" of the deep black background.
				</p>

				<div class="content-spark"></div>

				<h2>Final Thoughts</h2>
				<p>
					Conclude your manifesto with a strong closing statement. The spacing between sections is generous (72px - 144px) to let each part of your message resonate.
				</p>
			</article>
		</main>

		<!-- Footer -->
		<footer class="footer">
			<div class="container">
				<p class="footer-text">Axide <span class="accent">—</span> 2026</p>
			</div>
		</footer>
	</body>
</html>

<style>
	/* ===== CSS Variables ===== */
	:root {
		/* Neutrals */
		--primary-black: #0B0B10;
		--surface-black: #12121A;
		--primary-text: #F5F6FA;
		--secondary-text: #B9BBC7;
		--subtle-outline: #242433;

		/* Accent */
		--magenta: #E61E8C;
		--magenta-glow: rgba(230, 30, 140, 0.3);
		--magenta-subtle: rgba(230, 30, 140, 0.15);

		/* Spacing (8pt system) */
		--space-1: 8px;
		--space-2: 16px;
		--space-3: 24px;
		--space-4: 32px;
		--space-6: 48px;
		--space-9: 72px;
		--space-12: 96px;
		--space-18: 144px;
	}

	/* ===== Reset & Base ===== */
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	html {
		font-size: 18px;
		scroll-behavior: smooth;
	}

	body {
		font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
		font-weight: 400;
		background-color: var(--primary-black);
		color: var(--primary-text);
		line-height: 1.7;
		overflow-x: hidden;
	}

	/* ===== Typography ===== */
	h1, h2, h3 {
		font-family: 'Space Grotesk', sans-serif;
		font-weight: 700;
		line-height: 1.1;
		letter-spacing: -0.02em;
	}

	h1 {
		font-size: clamp(3rem, 10vw, 7rem);
	}

	h2 {
		font-size: clamp(2rem, 5vw, 3.5rem);
		margin-bottom: var(--space-9);
	}

	h3 {
		font-size: 1.5rem;
		font-weight: 600;
		margin-bottom: var(--space-2);
	}

	.accent {
		color: var(--magenta);
	}

	/* ===== Layout ===== */
	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 var(--space-4);
	}

	/* ===== Hero Section ===== */
	.hero {
		min-height: 100vh;
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		position: relative;
		overflow: hidden;
	}

	.logo-space {
		width: 100%;
		height: 50vh;
		min-height: 400px;
		display: flex;
		justify-content: center;
		align-items: center;
		position: relative;
	}

	.logo-container {
		width: clamp(300px, 40vh, 600px);
		height: clamp(300px, 40vh, 600px);
		position: relative;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.logo-container .logo {
		width: 100%;
		height: 100%;
		filter: drop-shadow(0 0 50px var(--magenta-glow));
	}

	/* Sparks - magenta micro-decorations */
	.spark {
		position: absolute;
		width: 40px;
		height: 3px;
		background: var(--magenta);
		border-radius: 2px;
		transform: rotate(-25deg);
		top: 20%;
		right: -10%;
		box-shadow: 0 0 20px var(--magenta-glow);
	}

	.spark--2 {
		width: 25px;
		height: 2px;
		top: 70%;
		left: -5%;
		transform: rotate(25deg);
	}

	.hero-content {
		text-align: center;
		padding: var(--space-6);
	}

	.diagonal-band {
		position: absolute;
		bottom: 0;
		left: -10%;
		width: 120%;
		height: 120px;
		background: var(--surface-black);
		transform: skewY(-3deg);
		transform-origin: left;
	}

	.diagonal-band--reverse {
		top: 0;
		bottom: auto;
		transform: skewY(3deg);
	}

	/* ===== Manifesto Section ===== */
	.manifesto {
		background: var(--surface-black);
		padding: var(--space-18) 0;
		position: relative;
	}

	.manifesto-lead {
		font-size: clamp(1.5rem, 4vw, 2.5rem);
		font-family: 'Space Grotesk', sans-serif;
		font-weight: 600;
		line-height: 1.4;
		max-width: 800px;
		text-align: center;
		margin: 0 auto;
	}
Long-form Content ===== */
	.content-wrapper {
		background: var(--surface-black);
		padding: var(--space-18) 0;
		position: relative;
	}

	.prose {
		max-width: 720px;
		margin: 0 auto;
		padding: 0 var(--space-4);
	}

	.prose .lead {
		font-size: clamp(1.25rem, 3vw, 1.75rem);
		font-family: 'Space Grotesk', sans-serif;
		font-weight: 600;
		line-height: 1.5;
		color: var(--primary-text);
		margin-bottom: var(--space-12);
	}

	.prose h2 {
		margin-top: var(--space-18);
		margin-bottom: var(--space-6);
		text-align: left;
	}

	.prose p {
		margin-bottom: var(--space-6);
		color: var(--secondary-text);
		font-size: 1.1rem;
	}

	.prose blockquote {
		margin: var(--space-12) 0;
		padding-left: var(--space-4);
		border-left: 3px solid var(--magenta);
		font-style: italic;
		text-align: left;
		font-size: 1.5rem;
	}

	.prose blockquote::before {
		display: none; /* Remove the previous centered quote style */
	}

	.prose ul {
		margin-bottom: var(--space-6);
		padding-left: var(--space-4);
		list-style: none;
	}

	.prose li {
		position: relative;
		margin-bottom: var(--space-2);
		color: var(--secondary-text);
	}

	.prose li::before {
		content: '';
		position: absolute;
		left: -20px;
		top: 12px;
		width: 8px;
		height: 2px;
		background: var(--magenta);
		transform: rotate(-25deg);
	}

	.content-spark {
		width: 60px;
		height: 3px;
		background: var(--magenta);
		margin: var(--space-12) 0;
		transform: rotate(-25deg);
		opacity: 0.5;
	}

	/* ===== Footer ===== */
	.footer {
		padding: var(--space-18) 0;
		background: var(--primary-black);
		border-top: 1px solid var(--subtle-outline);
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.footer-text {
		text-align: center;
		font-family: 'Space Grotesk', sans-serif;
		font-weight: 600;
		font-size: 1rem;
		color: var(--secondary-text);
		letter-spacing: 0.1em;
		text-transform: uppercase;
	}

	/* ===== Responsive ===== */
	@media (max-width: 768px) {
		.principle {
			grid-template-columns: 1fr;
			gap: var(--space-2);
		}

		.principle-number {
			font-size: 2rem;
		}

		.logo-space {
			height: 30vh;
			min-height: 200px;
		}
	}
</style>
